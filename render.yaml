services:
  # Main API Server
  - type: web
    name: mirrormind-api
    env: node
    rootDir: .
    buildCommand: npm run build:api
    startCommand: npm run start:api
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production

  # ML Microservice
  - type: web
    name: mirrormind-ml
    env: python
    rootDir: .
    buildCommand: pip install -r ml/requirements.txt
    startCommand: uvicorn ml.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHONPATH
        value: .
